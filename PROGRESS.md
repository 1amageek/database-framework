# Progress

- [x] FT15-01 The FullText packed-posting performance contract and reproducible benchmark dependency graph are reviewed and committed as a content-only change without changing query semantics `depends:none` `parallel:none`
- [ ] FT15-02 Canonical FullText posting scans and every module caller share one committed packed-suffix algebra with no repeated tuple unpack/elements/pack cycle and unchanged observable behavior `depends:FT15-01` `parallel:none`
  - [ ] FT15-02.1 TDD red evidence rejects the repeated decode/repack path by covering packed multi-element identifier ordering, intersection, union de-duplication, admission-before-retention failure, exact reservation release after success and failure, and malformed packed suffix failure at the scan decode boundary even when that candidate would later be excluded by intersection or union `depends:FT15-01` `parallel:none`
  - [ ] FT15-02.2 The implementation validates and decodes each scanned suffix once, then constructs one candidate that owns both the decoded tuple and its exact self-contained packed suffix; `FullTextPostingListAlgebra` compares and transfers the packed member, admission accounts for the complete candidate before append, and final retained-key construction consumes the already-decoded tuple without re-packing or re-decoding; `FullTextReadExecutors`, `FullTextIndexMaintainer`, and direct FullText query execution use this same algebra and preserve their established output boundaries `depends:FT15-02.1` `parallel:none`
  - [ ] FT15-02.3 A semantic review traces regular and polymorphic AND/OR public query paths and proves identical result set, storage order, duplicate removal, requested limit, authorization-before-read, bounded SQLite access, typed corrupt/missing failure, cursor cleanup, cancellation, and one-meter lifetime behavior `depends:FT15-02.2` `parallel:ft15-review`
  - [ ] FT15-02.4 A performance and ownership review proves the merge/admission loop contains no tuple decode, existential element-array materialization, or tuple re-pack; the sole necessary suffix detachment is bounded and documented, no pointer escapes, and changed common source introduces no target-specific synchronization or unchecked shared-state branch `depends:FT15-02.2` `parallel:ft15-review`
  - [ ] FT15-02.5 The executor resolves only concrete FT15-02 review findings, then runs one focused green lane for the changed algebra and canonical FullText read contracts with the pinned Xcode harness, exact intentional test count, and zero failures, skips, expected failures, compiler/analyzer/runtime warnings, internal errors, or retained-resource leaks `depends:FT15-02.3,FT15-02.4` `parallel:none`
  - [ ] FT15-02.6 The task designer approves the actual source, public execution path, tests, and focused evidence against FT15-01; the executor confirms branch, upstream, and a content-only commit message before committing this sprint, with no benchmark-driven limit pushdown or adjacent optimization added `depends:FT15-02.5` `parallel:none`
- [ ] FT15-03 The committed FullText optimization passes one final correctness integration and one uncontended official benchmark, is pushed to the configured current-branch upstream, and performs no release operation `depends:FT15-01,FT15-02` `parallel:none`
  - [ ] FT15-03.1 The complete `FullTextIndexTests` target runs once with the committed URL graph and exact post-change test count, proving zero failures, skips, expected failures, compiler/analyzer/runtime warnings, internal errors, or resource leaks; unchanged evidence is not rerun `depends:FT15-01,FT15-02` `parallel:none`
  - [ ] FT15-03.2 `scripts/xcode-benchmark-harness fulltext` runs once in Release on macOS arm64 through the real SQLite public query path with one test, two metrics, 15 samples per metric, successful 4,000-document correctness checks, and zero failures, skips, expected failures, compiler/analyzer/runtime warnings, or internal errors; medians must be intersection <= 12068.689 us and union <= 6698.496 us, otherwise this item remains open and the task designer revises the measured bottleneck contract before any new implementation or rerun `depends:FT15-03.1` `parallel:none`
  - [ ] FT15-03.3 The task designer confirms every invariant and both independent 15 percent thresholds from the preserved xcresult and raw logs; the executor verifies clean source, unchanged manifests, no unintended path dependencies, branch/upstream and commit message, commits the final progress state, and normally pushes `codex/df06d` only when no task-external unpushed commit is present `depends:FT15-03.2` `parallel:none`

# Progress

- [x] FT15-01 The FullText packed-posting performance contract and reproducible benchmark dependency graph are reviewed and committed as a content-only change without changing query semantics `depends:none` `parallel:none`
- [ ] FT15-02 Canonical FullText posting scans and every module caller share one committed canonical-key algebra with no merge-time tuple unpack/elements/pack cycle, exact candidate footprint reuse, and unchanged observable behavior `depends:FT15-01` `parallel:none`
  - [x] FT15-02.1 Revised TDD red tests are present for packed multi-element ordering, intersection, union de-duplication, decoder-accepted non-canonical integer equivalence with canonical identifiers, malformed suffix failure before algebraic exclusion, admission-before-retention failure, nested and long-payload exact-footprint reuse without count-based estimation, exact reservation release after success and failure, and cancellation after a partial scan with cursor finish and zero retained rows or bytes; the focused target compile reproduced the pre-implementation `canonicalKey`/`retainedFootprint` contract failures in `/private/tmp/df15-red-compile/build.log`, while the preserved Xcode lane reached only unrelated existing SQLite test compile blockers in `/private/tmp/df15-red-revised-results/Build-20260828-094526-83815.log` `depends:FT15-01` `parallel:none`
  - [x] FT15-02.2 The implementation validates and decodes each scanned suffix once, admits and creates one canonical comparison key once at the scan boundary, and constructs one candidate containing the decoded tuple, canonical key, and exact admitted footprint sidecar; `FullTextPostingListAlgebra` compares the canonical key and reuses the sidecar verbatim for successor admission, while final retained-key construction consumes the tuple and known canonical byte count without merge/final re-packing or re-decoding; `FullTextReadExecutors`, `FullTextIndexMaintainer`, and direct FullText query execution use this same algebra and preserve their established output boundaries and canonicalized equality `depends:FT15-02.1` `parallel:none`
  - [x] FT15-02.3 A semantic review traces regular and polymorphic AND/OR public query paths and proves identical logical result sets for canonical and decoder-accepted non-canonical identifiers, storage order, duplicate removal, requested limit, authorization-before-read, bounded SQLite access, typed malformed/missing failure, cursor cleanup, cancellation, and one-meter lifetime behavior `depends:FT15-02.2` `parallel:ft15-review`
  - [x] FT15-02.4 A performance and ownership review proves the scan performs at most one tuple decode and one canonical pack per candidate; merge/admission/final-retention contain no tuple decode, existential element-array materialization, tuple re-pack, or footprint re-estimation; the sole necessary source-suffix detachment is bounded, candidate sidecar accounting exactly matches the scan claim and successor admission for nested and long payloads, no pointer escapes, and changed common source introduces no target-specific synchronization or unchecked shared-state branch `depends:FT15-02.2` `parallel:ft15-review`
  - [x] FT15-02.5 The executor resolved only concrete FT15-02 review findings, then completed the focused pinned-Xcode green evidence: algebra 8/8 and partial-cancellation 1/1 passed, with zero failures, skips, expected failures, runtime warnings, or internal errors; build and raw logs are preserved beside `/private/tmp/df15-ft02-focused-resume.CzyeFX/Test-20260828-103137-20118.xcresult` and `/private/tmp/df15-ft02-cancel.61Ygcb/Test.xcresult` (9 intentional tests total) `depends:FT15-02.3,FT15-02.4` `parallel:none`
  - [x] FT15-02.6 The task designer approved the actual source, public execution path, tests, and focused evidence against FT15-01; the executor confirmed branch/upstream and the content-only commit message before committing this sprint, with no benchmark-driven limit pushdown or adjacent optimization added `depends:FT15-02.5` `parallel:none`
- [ ] FT15-03 The committed FullText optimization passes one final correctness integration and one uncontended official benchmark, is pushed to the configured current-branch upstream, and performs no release operation `depends:FT15-01,FT15-02` `parallel:none`
  - [ ] FT15-03.1 The complete `FullTextIndexTests` target runs once with the committed URL graph and exact post-change test count, proving zero failures, skips, expected failures, compiler/analyzer/runtime warnings, internal errors, or resource leaks; unchanged evidence is not rerun `depends:FT15-01,FT15-02` `parallel:none`
  - [ ] FT15-03.2 `scripts/xcode-benchmark-harness fulltext` runs once in Release on macOS arm64 through the real SQLite public query path with one test, two metrics, 15 samples per metric, successful 4,000-document correctness checks, and zero failures, skips, expected failures, compiler/analyzer/runtime warnings, or internal errors; medians must be intersection <= 12068.689 us and union <= 6698.496 us, otherwise this item remains open and the task designer revises the measured bottleneck contract before any new implementation or rerun `depends:FT15-03.1` `parallel:none`
  - [ ] FT15-03.3 The task designer confirms every invariant and both independent 15 percent thresholds from the preserved xcresult and raw logs; the executor verifies clean source, unchanged manifests, no unintended path dependencies, branch/upstream and commit message, commits the final progress state, and normally pushes `codex/df06d` only when no task-external unpushed commit is present `depends:FT15-03.2` `parallel:none`

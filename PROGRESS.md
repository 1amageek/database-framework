# Progress

- [x] FT15-01 The FullText packed-posting performance contract and reproducible benchmark dependency graph are reviewed and committed as a content-only change without changing query semantics `depends:none` `parallel:none`
- [x] FT15-02 Canonical FullText posting scans and every module caller share one committed canonical-key algebra with no merge-time tuple unpack/elements/pack cycle, exact candidate footprint reuse, and unchanged observable behavior; completed with commit `dc98958e` `depends:FT15-01` `parallel:none`
- [ ] FT15-02A Admission-aware canonical Tuple packing removes the one remaining repeated encoding measurement from every FullText posting candidate without changing Tuple bytes, query semantics, ownership, failure behavior, or scan limits `depends:FT15-01,FT15-02` `parallel:none`
  - [x] FT15-02A.1 The preserved official benchmark at source `53b0469197` proves the first sprint improved intersection by 8.905 percent and union by 9.293 percent but missed both 15 percent thresholds, while the public regular query path shows every scanned candidate still calls `packedByteCount` before `pack()`, causing one redundant canonical-encoding measurement in both metrics; evidence is `/private/tmp/df15-ft03-benchmark.LEj7wD/` and the same benchmark is not rerun in this sprint `depends:FT15-01,FT15-02` `parallel:none`
  - [x] FT15-02A.2 StorageKit package, `StorageKit` module, and Tuple component design authorities define one public admission-aware Tuple pack contract: measure the exact canonical byte count once, invoke the synchronous non-retained admission callback before allocation, then allocate once and encode once; callback failure allocates no packed result and crosses unchanged, and callers never infer an upper bound from source bytes; typed-throws contract documented in `9df2122` `depends:FT15-02A.1` `parallel:none`
  - [x] FT15-02A.3 TDD in `StorageKitTests` proves successful admission returns bytes identical to `pack()` with exactly one measure and one encode traversal, rejected admission invokes only the measure traversal and preserves the typed callback error, then StorageKit implements, reviews, commits, and normally pushes this Tuple-owned API without backend, transaction, or encoding-format changes; focused result 2/2 passed with zero failures/skips/expected failures/runtime warnings/internal errors, commit `9df2122` pushed to `origin/codex/ft15-tuple-pack` `depends:FT15-02A.2` `parallel:none`
  - [x] FT15-02A.4 database-framework pins the committed StorageKit revision and `FullTextPostingCandidate` replaces `packedByteCount` plus `pack()` with the admission-aware pack call, preserving one decode, exact footprint sidecar accounting, canonical/non-canonical equality, malformed/admission failure, cursor cleanup, regular/polymorphic AND/OR behavior, and zero retained resources; focused existing contract tests pass once with exact counts and zero failures, skips, expected failures, runtime warnings, or internal errors `depends:FT15-02A.3` `parallel:none`
  - [ ] FT15-02A.5 The task designer approves the StorageKit API implementation and actual FullText public path against both designs, confirms no limit pushdown, adjacent optimization, encoding assumption, path dependency, synchronization divergence, or incomplete implementation entered the change, and both repositories commit their coherent sprint state without release operations `depends:FT15-02A.4` `parallel:none`
- [ ] FT15-03 The committed FullText optimization passes one final correctness integration and one uncontended official benchmark, is pushed to the configured current-branch upstream, and performs no release operation `depends:FT15-01,FT15-02,FT15-02A` `parallel:none`
  - [ ] FT15-03.1 The committed post-FT15-02A Sources/Tests and URL dependency graph are the source under test, and the complete `FullTextIndexTests` enumeration/test run proves the reviewed exact count with zero failures, skips, expected failures, compiler/analyzer/runtime warnings, internal errors, or resource leaks `depends:FT15-01,FT15-02,FT15-02A` `parallel:none`
  - [ ] FT15-03.2 `scripts/xcode-benchmark-harness fulltext` runs once in Release on macOS arm64 through the real SQLite public query path with one test, two metrics, 15 samples per metric, successful 4,000-document correctness checks, and zero failures, skips, expected failures, compiler/analyzer/runtime warnings, or internal errors; medians must be intersection <= 12068.689 us and union <= 6698.496 us, otherwise this item remains open and the task designer revises the measured bottleneck contract before any new implementation or rerun `depends:FT15-03.1` `parallel:none`
  - [ ] FT15-03.3 The task designer confirms every invariant and both independent 15 percent thresholds from the preserved xcresult and raw logs; the executor verifies clean source, unchanged manifests, no unintended path dependencies, branch/upstream and commit message, commits the final progress state, and normally pushes `codex/df06d` only when no task-external unpushed commit is present `depends:FT15-03.2` `parallel:none`
